# Os pacotes @constructpluseu/* vivem no GitHub Packages, não no npm público.
# O token NUNCA é escrito aqui — é lido da variável de ambiente GITHUB_TOKEN.
# Ver secção "Design system" no README.
@constructpluseu:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
